你是一位资深架构师。
现在基于之前生成的 PROJECT_MAP。
请只分析：
【从以下模块中选择一个填入，每次只分析一个】
- Chat（对话）模块：AI 入口、意图识别、路由、通用对话、记忆触发
- Plan（计划）模块：计划制定、目标曲线展开、打卡、完成率、调整提案
- Memory（记忆）模块：长/中/短期分层记忆，提取→评分→去重→向量化存储与召回
- Diet（饮食）模块：自然语言饮食解析、营养计算、记录入库
- Knowledge/RAG（知识库）模块：食物营养库 + 健康文档库的向量检索
- Body（身体数据）模块：体重/围度/睡眠/运动/饮水/排便 记录与趋势统计
- Suggestion（建议）模块：每日建议/餐食建议/趋势洞察 的 LLM 生成与缓存
- User（用户系统）模块：健康档案、偏好、健康信息、设置、Onboarding
不要分析其它模块。
目标：
让我作为刚接手项目的开发人员，
彻底理解该模块的功能、代码流程和业务逻辑。
请输出：

# 一、模块职责
说明：
该模块解决什么问题。
处于整个系统什么位置。
上下游是谁。

# 二、功能清单
列出：
该模块所有功能。
并说明：
功能目的
业务价值

# 三、代码结构分析
说明：
Controller
Service
Manager
DAO
Consumer
Producer
Scheduler
分别负责什么。

# 四、接口清单
列出该模块所有接口：
接口名称
接口路径
请求方式
功能说明


# 五、Agent分析
如果模块涉及LangGraph：
请分析：
Graph
Node
State
Tool
Memory
Conditional Edge
分别承担什么职责。


# 六、完整执行流程
从用户请求开始分析：
User

↓

Controller

↓

Service

↓

Graph

↓

Node

↓

Tool

↓

Redis

↓

MySQL

↓

MQ

↓

返回结果
详细描述每一步。

# 七、Mermaid时序图
必须输出完整时序图。
例如：
sequenceDiagram

User->>Controller: 发起请求

Controller->>Graph: 启动Agent

Graph->>PlannerNode: 规划

PlannerNode->>LLM: 调用模型

LLM-->>PlannerNode: 返回计划

PlannerNode->>Tool: 查询数据

Tool->>MySQL: 查询

MySQL-->>Tool: 返回数据

Tool-->>PlannerNode: 返回结果

PlannerNode->>SummaryNode: 汇总

SummaryNode->>LLM: 生成答案

LLM-->>SummaryNode: 返回答案

SummaryNode-->>User: 输出结果

# 八、数据库分析
列出：
涉及的数据表
表作用
核心字段
表之间关系
数据流转过程

# 十一、异常处理分析
说明：
异常捕获
重试机制
补偿机制
幂等方案

# 十二、项目亮点
请从代码中挖掘：
技术亮点
架构亮点
性能优化点
可扩展性设计

# 十三、面试讲解版
假设我是项目负责人。
请给我：
3分钟讲解版本
10分钟讲解版本

# 十四、新人阅读路线
请告诉我：
如果只看20%的代码，
应该优先阅读哪些文件。
并说明原因。
要求：

1. 所有结论必须基于代码。
2. 推断内容必须标注【推断】。
3. 优先分析业务流程，而不是代码细节。
4. 必须画出时序图。
5. 必须说明数据流向。
6. 必须说明上下游依赖关系。

# 十五、带我读代码的流程
我要在你输出上述分析之后，亲自把这个模块的代码读一遍。
请为我设计一个"循序渐进的读码流程"，要求：

1. 按照"从外到内、从主干到分支"的顺序，把该模块的关键文件排成一个有序的阅读清单。
   - 每个文件标注：文件路径、它在本模块中的角色（Controller/Service/Node/State/Repository/Model 等）、为什么放在这个阅读顺序上。
   - 顺序原则建议：入口路由 → 请求/响应 Schema → Service 编排 → Agent Graph 装配 → State 定义 → 各 Node → Tool/检索 → Repository → Model。

2. 把阅读过程拆成若干"阅读阶段"（如：阶段1 跑通主链路、阶段2 看 Agent 内部、阶段3 看数据落库），
   每个阶段说明：本阶段目标、要读哪几个文件、读完后我应该能回答的问题（自检清单）。

3. 对每个关键文件，给我"阅读时重点看什么"的提示（如：看这个 Node 的输入输出、看这条 Conditional Edge 的判断条件、看这个 Repository 如何做用户隔离），
   避免我陷入逐行细节。

4. 给出一条"最短验证路径"：我可以追踪哪一个具体请求（从某个接口进入），一路跟到数据库落库 / SSE 返回，
   把这条链路上依次经过的文件按调用顺序列出来，让我用一个真实请求把整个模块串起来。

5. 标注"可以暂时跳过"的文件（如边缘分支、异常兜底、不常用功能），让我先抓主干。

要求：阅读清单中的文件路径必须真实存在于代码库中，不要编造。